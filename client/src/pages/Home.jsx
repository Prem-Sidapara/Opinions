import React from 'react';
import Feed from '../components/Feed';

const Home = () => {
    return (
        <div className="container relative">
            <Feed />
        </div>
    );
};

export default Home;
